# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_get_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Retrieve Appointment Scenario"
    flow:
      - log: "Starting Retrieve Appointment Test"
      - get:
          url: "/appointment/{{ appointmentId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          statusCode: 200
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty:
            - "control"
            - "control.type"
            - "control.message"
            - "control.code"
            - "appointments"
            - "appointments.id"
            - "appointments.lifeCycleStatus"
            - "appointments.occurrences"
      - expect:
          equals:
            - control.type: "S"
            - control.code: 200
            - control.message: "Ok"
            - appointments.id: "SA-562403"
            - appointments.lifeCycleStatus: "Não atribuído"
      - expect:
          jmespath:
            - "appointments.occurrences[0].creationDateTime == '2021-12-07T12:17:20'"
            - "appointments.occurrences[0].startDate == '2021-10-09T13:00:00'"
            - "appointments.occurrences[0].finishDate == '2021-10-09T18:00:00'"
            - "appointments.occurrences[0].reason == 'Teste Vtal CW'"
            - "appointments.occurrences[0].reasonDetail == 'Teste Vtal CW'"
      - think: 2
