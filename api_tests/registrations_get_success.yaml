# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields: ["size", "offset", "recipient-id", "credential-url"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Test API behavior with valid input payloads"
    flow:
      - get:
          url: "/registrations?size={{ size }}&offset={{ offset }}&recipient-id={{ recipient-id }}&credential-url={{ credential-url }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - equals: ["registrations[0].id"]
            - equals: ["registrations[0].reference"]
            - equals: ["registrations[0].startDate"]
            - equals: ["registrations[0].endDate"]
            - equals: ["registrations[0].status"]
            - equals: ["registrations[0].credentialUrl"]
            - equals: ["registrations[0].suspendedUntil"]
            - equals: ["registrations[0].issuedAt"]
            - equals: ["registrations[0].createdAt"]
            - equals: ["registrations[0].updatedAt"]
            - equals: ["registrations[0].recipient.id"]
            - equals: ["registrations[0].recipient.name"]
            - equals: ["registrations[0].recipient.type"]
      - think: 1
