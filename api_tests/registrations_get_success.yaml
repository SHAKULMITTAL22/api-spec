# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "size"
      - "offset"
      - "recipient_id"
      - "credential_url"
  plugins:
    expect: {}

scenarios:
  - name: "GET /registrations"
    flow:
      - get:
          url: "/registrations?size={{ size }}&offset={{ offset }}&recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "registrations"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - equals: ["headers.api-version", "1.0.0"]
          think: 1
