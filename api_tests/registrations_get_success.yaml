# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields: ["size", "offset", "recipient_id", "credential_url"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Token: "{{$processEnvironment.AUTH_TOKEN}}"
      
scenarios:
  - name: "Test scenario for /registrations endpoint"
    flow:
      - get:
          url: "/registrations?size={{ size }}&offset={{ offset }}&recipient_id={{ recipient_id }}&credential_url={{ credential_url }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: ['registrations', 'total']
            - hasHeader: ['api-version', 'page-size', 'page-offset']
        think: 1
