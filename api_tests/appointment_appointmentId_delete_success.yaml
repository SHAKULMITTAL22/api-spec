# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=8ae4a840f9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_delete_success.csv"
    fields:
      - appointmentId
      - appointmentReason
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Delete Appointment Scenario"
    flow:
      - log: "Starting Delete Appointment Scenario"
      - think: 
          seconds: 2
      - log: "Attempting to Delete Appointment with ID: {{ appointmentId }}"
      - delete:
          url: "/appointment/{{ appointmentId }}?appointmentReason={{ appointmentReason }}&appointmentComments={{ appointmentComments }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 200
          - contentType: application/json
          - hasHeader: content-type
          - headerEquals:
              content-type: application/json
          - hasProperty: control.type
          - hasProperty: control.code
          - hasProperty: control.message
          - equals:
              control.type: "S"
          - equals:
              control.code: 200
          - equals:
              control.message: "OK"
      - think: 
          seconds: 2
