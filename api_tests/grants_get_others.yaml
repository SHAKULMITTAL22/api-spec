# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: "API failure cases"
    flow:
      # Testing with invalid URL
      - get:
          url: "/invaliurl"
          expect:
            - statusCode: 404
      - think: 5

      # Testing with missing authentication token
      - get:
          url: "/grants"
          headers:
            Token: ""
          expect:
            - statusCode: 401
      - think: 5

      # Testing with incorrect authentication token
      - get:
          url: "/grants"
          headers:
            Token: "incorrect_token"
          expect:
            - statusCode: 403
      - think: 5

      # Testing with correct authentication token
      - get:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
      - think: 5
