# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_delete for http method type DELETE in artillery framework
# RoostTestHash=8ae4a840f9
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_delete_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses for Delete Appointment"
    flow:
      - log: "Scenario: Testing invalid payloads for Delete Appointment"
      
      - log: "Request: Invalid appointmentId format"
      - delete:
          url: "/appointment/INVALID_ID?appointmentReason=Cancelamento Checkout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Request: Missing required query parameter appointmentReason"
      - delete:
          url: "/appointment/SA-562403"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Request: Unauthorized request"
      - delete:
          url: "/appointment/SA-562403?appointmentReason=Cancelamento Checkout"
          headers:
            Authorization: "Bearer INVALID_TOKEN"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Request: Non-existent appointmentId"
      - delete:
          url: "/appointment/SA-000000?appointmentReason=Cancelamento Checkout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Request: Invalid URL"
      - delete:
          url: "/invalid_endpoint/SA-562403?appointmentReason=Cancelamento Checkout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2
