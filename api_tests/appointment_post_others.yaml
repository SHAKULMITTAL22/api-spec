# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment_post for http method type POST in artillery framework
# RoostTestHash=b7eaf9bc25
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests for Appointment Creation"
    flow:
      - log: "Attempting to create appointment with invalid slot ID"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "invalid-slot-id"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          statusCode: 400
          hasProperty: "error"
          hasProperty: "message"
          contentType: "application/json"
      - think: 2

      - log: "Attempting to create appointment with missing slot ID"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot: {}
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          statusCode: 400
          hasProperty: "error"
          hasProperty: "message"
          contentType: "application/json"
      - think: 2

      - log: "Attempting to create appointment with missing reason"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
      - expect:
          statusCode: 400
          hasProperty: "error"
          hasProperty: "message"
          contentType: "application/json"
      - think: 2

      - log: "Attempting to create appointment with invalid URL"
      - post:
          url: "/invalid_appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          statusCode: 404
          hasProperty: "error"
          hasProperty: "message"
          contentType: "application/json"
      - think: 2

      - log: "Attempting to create appointment without authorization"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
          json:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          statusCode: 401
          hasProperty: "error"
          hasProperty: "message"
          contentType: "application/json"
      - think: 2
