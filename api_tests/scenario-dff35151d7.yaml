# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "scenario-dff35151d7.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create my account"
    flow:
      # POST request to create an account
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "{{ commit-hash }}"
          json:
            email: "{{ email }}"
          expect:
            # Expecting a successful operation
            - statusCode: 201
            - hasProperty: "id"
            # Asserting the API version and commit hash in the response headers
            - headerEquals:
                api-version: "1.0.0"
                commit-hash: "{{ commit-hash }}"
          log: "POST /accounts - Create an account with the specified information"
      - think: 1
      # GET request to verify the created account
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Token: "{{ Token }}"
          expect:
            # Expecting a successful operation
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            # Asserting the API version in the response headers
            - headerEquals:
                api-version: "1.0.0"
          log: "GET /me - Verify the account created"
      - think: 1
