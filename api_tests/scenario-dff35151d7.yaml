# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
# 
# 
# ********RoostGPT********

// Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
// 
config:
  target: "{{ $processEnvironment.API_HOST }}" // Ensure the API_HOST environment variable is set correctly
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "scenario-dff35151d7.csv" // Ensure the csv file exists at the specified path and has the correct format
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create my account"
    flow:
      - post:
          url: "/accounts" // Ensure the endpoint "/accounts" exists and is expecting a POST request
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" // Ensure the AUTH_TOKEN is set correctly
            commit-hash: "{{ payload.commit-hash }}" // Ensure the commit-hash is provided in the payload
          json:
            email: "{{ payload.email }}" // Ensure the email is provided in the payload
          expect:
            - statusCode: 201
            - hasProperty: "api-version"
            - hasProperty: "commit-hash" // Ensure the response includes these properties
          log: "POST /accounts request sent"
      - think: 1

      - get:
          url: "/me" // Ensure the endpoint "/me" exists and is expecting a GET request
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}" // Ensure the AUTH_TOKEN is set correctly
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "api-version" // Ensure the response includes these properties
          log: "GET /me request sent"
      - think: 1
