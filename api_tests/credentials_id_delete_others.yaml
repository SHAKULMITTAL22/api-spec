# ********RoostGPT********
#
Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4

================================VULNERABILITIES================================
Vulnerability:N/A
Issue: N/A
Solution: N/A

================================================================================
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7


#

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
      
scenarios:
  - name: "Test scenario for various non-successful API requests"
    flow:
      # Testing DELETE request with invalid UUID
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      # Testing DELETE request without UUID
      - delete:
          url: "/credentials/"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1

      # Testing DELETE request with incorrect URL
      - delete:
          url: "/invalid/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      # Testing DELETE request without authentication token
      - delete:
          url: "/credentials/{{ id }}"
          headers: {}
          expect:
            - statusCode: 401
      - think: 1
