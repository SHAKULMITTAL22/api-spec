# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: '{{ $processEnvironment.API_HOST }}'
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: 'credentials_id_delete_others.csv'
    fields:
      - 'id'
    skipHeader: true

scenarios:
  - name: 'Testing non-success API responses'
    flow:
      # Testing with invalid authorization token
      - post:
          url: '/credentials/{{ id }}'
          headers:
            Token: '{{ $processEnvironment.INVALID_TOKEN }}'
          expect:
            - statusCode: 401

      - think: 1

      # Testing with missing required headers
      - post:
          url: '/credentials/{{ id }}'
          expect:
            - statusCode: 400

      - think: 1

      # Testing with invalid URL
      - post:
          url: '/invalid/url/{{ id }}'
          headers:
            Token: '{{ $processEnvironment.AUTH_TOKEN }}'
          expect:
            - statusCode: 404

      - think: 1

      # Testing with missing required payload
      - post:
          url: '/credentials/{{ id }}'
          headers:
            Token: '{{ $processEnvironment.AUTH_TOKEN }}'
          expect:
            - statusCode: 400

      - think: 1

      # Testing with invalid payload
      - post:
          url: '/credentials/{{ id }}'
          headers:
            Token: '{{ $processEnvironment.AUTH_TOKEN }}'
          json:
            invalid_key: 'invalid_value'
          expect:
            - statusCode: 400

      - think: 1
