# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "./accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Login to existing account"
    flow:
      - log: "LOGIN REQUEST: Sending login request for an existing account"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          capture:
            - json: "$.email"
              as: "loginEmail"
      - log: "Login response: {{ response.body }}"
      - assert:
          - equals:
              - "{{ response.status }}"
              - 200
      - assert:
          - contains:
              - "{{ loginEmail }}"
              - "{{ email }}"
      - think: 1
