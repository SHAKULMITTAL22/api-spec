# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
  plugins:
    expect: {}
scenarios:
  - name: "Delete one credential from my Wallet"
    flow:
      - log: "Starting API test scenario for deleting a credential."
      - delete: 
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 200
      - expect:
          - hasHeader: "api-version"
      - expect:
          - headerEquals: 
              name: "api-version"
              value: "1.0.0"
      - think: 1
      - log: "API test scenario for deleting a credential completed successfully."
