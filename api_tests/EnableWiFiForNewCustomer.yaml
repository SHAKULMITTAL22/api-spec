# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type DBRX and AI Model dbrx-instruct-032724
# 
# ROOST_METHOD_HASH=4992e43bd0
# ROOST_METHOD_SIG_HASH=cb107cc3ec
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: WiFi Troubleshooting for Fiber Internet Customers',
#   background: 'Background:\n' +
#     '    Given the customer support representative has access to the service activation and configuration API\n' +
#     '    And the customer support representative has access to the service test management API',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Enable WiFi for a new customer',
#     steps: 'Given a new customer with subscriber ID "SVTAL005" has just had their fiber service installed\n' +
#       'When the support representative uses the configuration API to enable WiFi\n' +
#       'Then the configuration API should return a success response\n' +
#       'And the customer should be able to see and connect to their new WiFi network',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    expect: {}

scenarios:
  - name: "Enable WiFi for a new customer"
    flow:
      - log: "Given a new customer with subscriber ID {{ subscriberID }} has just had their fiber service installed"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
              id: "{{ subscriberID }}"
        expect:
          - statusCode: 200
            body:
              status: "success"
              control:
                type: "S"
                message: "WiFi enabled successfully"
                code: "200"
        log: "When the support representative uses the configuration API to enable WiFi, then the configuration API should return a success response and control message should be 'WiFi enabled successfully'"

  - name: "Check customer WiFi connection"
    flow:
      - log: "And the customer should be able to see and connect to their new WiFi network"
        think: 1
