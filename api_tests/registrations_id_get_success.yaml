# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_get for http method type GET in artillery framework

# RoostTestHash=f67303cb4b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "registrations_id_get_success.csv"
    fields:
      - "id"
  plugins:
    expect: {}

scenarios:
  - name: "Registration Scenario"
    flow:
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "recipient"
            - hasProperty: "recipient.id"
            - hasProperty: "recipient.name"
            - hasProperty: "recipient.type"
          capture:
            - json: "$.id"
              as: "registrationId"
        # Think time
        think: 5
