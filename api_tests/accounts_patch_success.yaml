# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}

scenarios:
  - name: "Successful Login to Existing Account"
    flow:
      - log: "Sending POST request to /accounts endpoint to login to existing account"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          payload:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: email
            - equals:
                - "email"
                - "{{ email }}"
            - hasHeader: "api-version"
            - headerEquals:
                - "api-version"
                - "1.0.0"
      - log: "Received 200 OK response for successful login to existing account"
      - think: 2

payload:
  path: "accounts_patch_success.csv"
  fields:
    - "email"
  skipHeader: true
  delimiter: "^|^"
