# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
target: "{{ $processEnvironment.API_HOST }}"

requests:
  - url: "/accounts"
    method: "POST" 
    authorization:
      type: "Bearer"
      token: "{{$processEnvironment.AUTH_TOKEN}}"
    payload:
      $ref: "#/components/schemas/accountsPayload"

components:
  schemas:
    accountsPayload:
      type: "delimited-data" 
      dataPath: "accounts_patch_success.csv"
      skipHeader: true
      delimiter: "^|^"
      fields:
        - name: "email"
          type: "string"
          format: "email"
          required: true
