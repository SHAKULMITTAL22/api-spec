# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account"
    flow:
      - log: "Sending a POST request to create a new account with valid email"
      - think: 2
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abc1234"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: "data"
            - equals:
                - "data.email"
                - "{{ email }}"
      - log: "Account created successfully"

      - log: "Sending a POST request to create a new account with invalid email format"
      - think: 2
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abc1234"
          json:
            email: "invalidemail"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error.message"
                - "Invalid email format"
      - log: "Account creation failed with invalid email format"

      - log: "Sending a POST request to create a new account without email"
      - think: 2
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abc1234"
          json: {}
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error.message"
                - "Email is required"
      - log: "Account creation failed without email"
