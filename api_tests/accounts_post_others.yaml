# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  variables:
    invalidEmail: "invalidemail"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
      - "invalidEmail"
    delimiter: "^|^"
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses"
    flow:

      - log: "Scenario: Create new Account with invalid email format"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          payload:
            email: "{{ invalidEmail }}"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: message
            - equals:
                - "message"
                - "Invalid email format"
      - think: 2

      - log: "Scenario: Create new Account with missing email field" 
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          payload: {}
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: message
            - equals:
                - "message"
                - "Email is required"
      - think: 2

      - log: "Scenario: Create new Account with invalid commit hash header"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            commit-hash: "invalid"
          payload:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: message
            - matchesRegexp:
                - "message"
                - "Commit hash header must match pattern ^[a-z0-9]{7}$"
      - think: 2
