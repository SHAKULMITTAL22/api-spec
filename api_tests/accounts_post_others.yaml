# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 5
      name: Warm up
    - duration: 30 
      arrivalRate: 5
      rampTo: 50
      name: Ramp up load  
    - duration: 60
      arrivalRate: 50 
      name: Sustained load

scenarios:
  - name: "Account Creation"
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload:
            email: "{{ randomEmail }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: email
            - equals:
                - "email"
                - "{{ randomEmail }}"
          log: "Sent POST request to /accounts with email: {{ randomEmail }}"
      - think: 2
      - log: "Account creation scenario completed"

  - name: "Account Creation - CSV Data"  
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload:
            $ref: "./accounts_post_others.csv"
            skipHeader: true
            delimiter: "^|^"
            fields:
              - randomEmail
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: email
          log: "Sent POST request to /accounts with email from CSV data"
      - think: 2
      - log: "Account creation scenario with CSV data completed"
