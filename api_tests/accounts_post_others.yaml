# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account"
    flow:

      - think: 2

      - log: "Creating a new account with valid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: "data"
            - equals:
                - "data.email"
                - "{{ email }}"

      - think: 1

      - log: "Attempting to create an account with invalid email format"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload:
            email: "invalidemail"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error.message"
                - "Invalid email format"

      - think: 1

      - log: "Attempting to create an account with missing email field"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload: {}
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error.message"
                - "Email is required"
