# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_patch for http method type PATCH in artillery framework
# RoostTestHash=33f5f2e325
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_patch_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: Test invalid API requests
    flow:
      - log: "Test invalid appointmentId format"
      - post:
          url: "/appointment/invalid-appointment-id"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 1

      - log: "Test missing slot id in payload"
      - post:
          url: "/appointment/SA-562403"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            appointment:
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 1

      - log: "Test invalid slot id format in payload"
      - post:
          url: "/appointment/SA-562403"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            appointment:
              slot:
                id: "invalid-slot-id"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 1

      - log: "Test missing authorization header"
      - post:
          url: "/appointment/SA-562403"
          headers:
            Content-Type: "application/json"
          payload:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 1

      - log: "Test invalid URL path"
      - post:
          url: "/invalid-appointment-path/SA-562403"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
              reason: "Agendamento para Instalação de Fibra"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 1
