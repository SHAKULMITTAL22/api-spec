# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
# 
# ================================VULNERABILITIES================================
# Vulnerability: Potential Information Disclosure
# Issue: The /credentials endpoint is accessed multiple times. This endpoint could potentially expose sensitive information if not properly secured. (Line 1, 2, 3)
# Solution: Ensure that the /credentials endpoint is secured with appropriate authentication and authorization checks. Also, consider encrypting sensitive data to add an additional layer of security.
# 
# ================================================================================
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "Validate successful API responses"
    flow:
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "credentials"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
      - think: 1
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
      - think: 1
