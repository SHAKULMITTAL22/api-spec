# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=2c75b43156


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 5
scenarios:
  - name: "Test scenario for successful API requests"
    flow:
      # GET request to fetch registration details
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
      # Think time after GET request
      - think: 5
      # DELETE request to delete registration
      - delete:
          url: "/registrations/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
      # Think time after DELETE request
      - think: 5
