# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_get for http method type GET in artillery framework

# RoostTestHash=f67303cb4b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Invalid Token Test"
        get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - log: "Missing Token Test"
        get:
          url: "/registrations/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - log: "Invalid URL Test"
        get:
          url: "/invalid/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - log: "Invalid ID Test"
        get:
          url: "/registrations/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1
