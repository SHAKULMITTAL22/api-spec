# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_get for http method type GET in artillery framework

# RoostTestHash=f67303cb4b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Invalid URL case"
      - get:
          url: "/invalidURL"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Invalid authentication token case"
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Missing authentication token case"
      - get:
          url: "/registrations/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Invalid payload case"
      - get:
          url: "/registrations/InvalidUUID"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Non-existing resource case"
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
