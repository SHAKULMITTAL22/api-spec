# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up
  plugins:
    expect: {}

scenarios:
  - name: "Login API Failure Tests"
    flow:
      - log: "Sending POST request with missing required fields in payload"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Missing required fields: client, otp"
        think: 2

      - log: "Sending POST request with invalid email format"
        post:
          url: "/login" 
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalidemail"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Invalid email format"
        think: 2
        
      - log: "Sending POST request with invalid client value"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "invalidclient"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Invalid client value. Allowed values: mobile, web, desktop, api"
        think: 2

      - log: "Sending POST request to incorrect URL"
        post:
          url: "/incorrect-login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 404
        think: 2

      - log: "Sending POST request without authorization header"
        post:
          url: "/login"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Unauthorized"
        think: 2
