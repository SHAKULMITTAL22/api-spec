# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Login Failure Tests"
    flow:
      - log: "Attempting login with missing required fields"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Missing required fields: client, otp"
      - think: 2

      - log: "Attempting login with invalid email format"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalid_email"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error"
                - "Invalid email format"
      - think: 2
      
      - log: "Attempting login with invalid client type"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "invalid"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains: 
                - "error"
                - "client must be one of [mobile, web, desktop, api]"
      - think: 2

      - log: "Attempting login with incorrect OTP"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "111111"
            client: "web"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Invalid OTP"
      - think: 2

      - log: "Attempting login with expired OTP"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com" 
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 401  
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "OTP expired"
