# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Login API Failure Tests"
    flow:
      - log: "Attempting login with invalid email format"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalidemail"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "INVALID_EMAIL_FORMAT"
      - think: 2

      - log: "Attempting login with missing required fields"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "MISSING_REQUIRED_FIELDS"
      - think: 2

      - log: "Attempting login with invalid OTP"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "invalid"
            client: "web"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "INVALID_OTP"
      - think: 2

      - log: "Attempting login with invalid client type"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "invalid"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "INVALID_CLIENT_TYPE"
      - think: 2

      - log: "Attempting login with unauthorized request"
        post:
          url: "/login"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "UNAUTHORIZED"
      - think: 2
