# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type DBRX and AI Model dbrx-instruct
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  # Removed processor key and its associated value as it is not allowed in the latest version of Artillery
  plugins:
    expect: {}

scenarios:
  - name: "Invalid requests for /login"
    flow:
      - log:
          message: "Scenario: Invalid requests for /login"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          payload:
            delimiter: "^|^"
            fields:
              - email
              - otp
              - client
            dataFile: "login_post_others.csv"
          expect:
            statusCode: 400
          onFailed:
            - log: "Failed request: {{ $request }}\nResponse: {{ $response }}"
      - think: 5
