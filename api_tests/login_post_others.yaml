# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  http:
    pool: 10

scenarios:
  - name: "Non-success API responses"
    flow:
      - post:
          url: "/login"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
          - equals: ["error", "Invalid credentials"]
          - equals: ["message", "Invalid OTP"]
      - think: 1

      - post:
          url: "/nonexistent"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
          - equals: ["error", "Not Found"]
          - equals: ["message", "URL does not exist"]
      - think: 1

      - post:
          url: "/login"
          headers:
            Token: "invalid_token"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 403
            - hasProperty: "error"
            - hasProperty: "message"
          - equals: ["error", "Forbidden"]
          - equals: ["message", "Invalid Token"]
      - think: 1
