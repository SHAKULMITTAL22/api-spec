# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "login_post_others.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Login API Test"
    flow:
      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 200
            - contentType: json
            - hasProperty: token
          capture:
            json: "$.token"
            as: "token"
      - log: "Received token: {{ token }}"

      - think: 2

      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalidemail"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: error
      - log: "Invalid email error: {{ error }}"

      - think: 2

      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "invalidotp"
            client: "{{ client }}"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: error
      - log: "Invalid OTP error: {{ error }}"
