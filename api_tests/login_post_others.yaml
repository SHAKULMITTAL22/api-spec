# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type DBRX and AI Model dbrx-instruct
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 10
  plugins:
    expect: {}

scenarios:
  - name: "Invalid login requests"
    flow:
      - log: "Requesting invalid login with incorrect email"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            client: "test-client"
            email: "invalid-email"
            otp: "123456"
          expect:
            statusCode: 400
            contentType: "application/json"
            hasProperty: "message"
            headerEquals:
              Content-Type: "application/json"
        think: 1

      - log: "Requesting invalid login with incorrect otp"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            client: "test-client"
            email: "test@example.com"
            otp: "invalid-otp"
          expect:
            statusCode: 400
            contentType: "application/json"
            hasProperty: "message"
            headerEquals:
              Content-Type: "application/json"
        think: 1

      - log: "Requesting invalid login with missing client"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
            otp: "123456"
          expect:
            statusCode: 400
            contentType: "application/json"
            hasProperty: "message"
            headerEquals:
              Content-Type: "application/json"
        think: 1

      - log: "Requesting invalid login with incorrect URL"
        post:
          url: "/invalid-login"
          headers:
            Content-Type: "application/json"
          json:
            client: "test-client"
            email: "test@example.com"
            otp: "123456"
          expect:
            statusCode: 404
            contentType: "text/html"
            headerEquals:
              Content-Type: "text/html"
        think: 1
