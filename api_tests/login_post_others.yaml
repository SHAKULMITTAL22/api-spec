# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  variables:
    email: "test@example.com"
    otp: "123456"
    client: "web"

scenarios:
  - name: "Login Failure Tests"
    flow:
      - log: "Attempting login with missing required fields"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Missing required fields: otp, client"
      - think: 2

      - log: "Attempting login with invalid email format"
        post:
          url: "/login" 
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalidemail"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - contains:
                - "error"
                - "Invalid email format"
      - think: 2
      
      - log: "Attempting login with incorrect OTP"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ email }}"
            otp: "000000"
            client: "{{ client }}"
          expect:
            - statusCode: 401
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error"
                - "Incorrect OTP"
      - think: 2

      - log: "Attempting login with unsupported client type"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "unsupported"
          expect:
            - statusCode: 400 
            - contentType: json
            - hasProperty: "error"
            - contains: 
                - "error"
                - "client must be one of [mobile, web, desktop, api]"
      - think: 2

      - log: "Attempting login with incorrect URL"
        post:
          url: "/wrong-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 404
