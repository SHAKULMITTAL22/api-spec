# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type DBRX and AI Model dbrx-instruct
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Incorrect API usage"
    flow:
      - log:
          message: "Test scenario: Incorrect API usage"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{email}}"
            otp: "{{otp}}"
            client: "{{client}}"
          expect:
            statusCode: 400
      - think:
          duration: 1

  - name: "Invalid URL"
    flow:
      - log:
          message: "Test scenario: Invalid URL"
      - get:
          url: "/nonexistent"
          headers:
            Content-Type: "application/json"
          expect:
            statusCode: 404
      - think:
          duration: 1

  - name: "Authentication failure"
    flow:
      - log:
          message: "Test scenario: Authentication failure"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "invalid_email@example.com"
            otp: "123456"
            client: "invalid_client"
          expect:
            statusCode: 401
      - think:
          duration: 1

  - name: "Missing request body"
    flow:
      - log:
          message: "Test scenario: Missing request body"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          expect:
            statusCode: 400
      - think:
          duration: 1
