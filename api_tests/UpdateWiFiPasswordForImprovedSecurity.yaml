# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# ROOST_METHOD_HASH=5a02b01fca
# ROOST_METHOD_SIG_HASH=9c8324b0ac
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: WiFi Troubleshooting for Fiber Internet Customers',
#   background: 'Background:\n' +
#     '    Given the customer support representative has access to the service activation and configuration API\n' +
#     '    And the customer support representative has access to the service test management API',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Update WiFi password for improved security',
#     steps: 'Given a customer with subscriber ID "SVTAL003" requests a WiFi password change\n' +
#       'When the support representative uses the configuration API to set a new WiFi password\n' +
#       'Then the configuration API should return a success response\n' +
#       'And the customer should be able to connect to the WiFi network with the new password',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processContext.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "UpdateWiFiPasswordForImprovedSecurity.csv"
    fields:
      - "newPassword"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Update WiFi password for improved security
    flow:
      - post:
          url: "/configuration"
          headers:
            Authorization: "Bearer {{ $processContext.AUTH_TOKEN }}"
          json:
            subscriberID: "SVTAL003"
            newPassword: "{{ newPassword }}"
            configuration:
              action:
                type: "HGW_WIFI_SET_PASSWD"
              id: "SVTAL003"
              adminStatus: "1"
              startDate: "{{ $processEnvironment.START_DATE }}"
              endDate: "{{ $processEnvironment.END_DATE }}"
              state: "active"
          log: "Sending request to update WiFi password for subscriber SVTAL003."
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - equals:
                path: "control.type"
                value: "S"
            - equals:
                path: "control.message"
                value: "Success"
            - equals:
                path: "control.code"
                value: "200"
        log: "Expecting a 200 OK response indicating success."
      - think: 1

      - post:
          url: "/serviceTest"
          headers:
            Authorization: "Bearer {{ $processContext.AUTH_TOKEN }}"
          json:
            serviceTest:
              type: "diagnostic"
              customer:
                subscriberId: "SVTAL003"
          log: "Sending service test request to check new WiFi password connectivity for subscriber SVTAL003."
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - equals:
                path: "control.type"
                value: "S"
            - equals:
                path: "control.message"
                value: "Connection Successful"
            - equals:
                path: "control.code"
                value: "200"
        log: "Expecting a 200 OK response and a positive connection status."
      - think: 1
