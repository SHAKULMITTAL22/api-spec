# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-success API responses testing"
    flow:
      # Invalid token in header
      - post:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401
          think: 1

      # Unauthorized access
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
          think: 1

      # Requesting non-existing resource
      - get:
          url: "/non_existing_resource?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1

      # Invalid payload
      - post:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalid_field: "invalid_value"
          expect:
            - statusCode: 400
          think: 1

      # Server error
      - get:
          url: "/server_error?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
          think: 1
