# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-successful API responses"
    flow:
      - # Incorrect URL
        get:
          url: "/credntials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

        think: 1

      - # Invalid payload
        post:
          url: "/credentials"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "invalid_password"
          expect:
            - statusCode: 400
            - hasProperty: "errors"

        think: 1

      - # Authentication issues
        get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401

        think: 1

      - # Testing without required headers
        get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"

        think: 1
