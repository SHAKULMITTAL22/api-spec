# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-success API responses testing"
    flow:
      - log: "Invalid API usage: Invalid payload"
      - post:
          url: "/credentials"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            password: "INCORRECT_PASSWORD" # Incorrect password
      - expect:
          - statusCode: 401 # Unauthorized due to incorrect password
      - think: 1

      - log: "Invalid API usage: Missing required header"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers: {}
      - expect:
          - statusCode: 401 # Unauthorized due to missing Token header
      - think: 1

      - log: "Invalid API usage: Incorrect URL"
      - get:
          url: "/incorrectUrl" # Incorrect URL
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
      - expect:
          - statusCode: 404 # Not Found due to incorrect URL
      - think: 1
