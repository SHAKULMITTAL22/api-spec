# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials_get for http method type GET in artillery framework

# RoostTestHash=c5011f3c64


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-success API responses testing"
    flow:
      - log: "Testing with invalid API key"
      - post:
          url: "/credentials?size=10&offset=0"
          headers:
            Token: "invalidToken"
          expect:
            - statusCode: 401
      - think: 2

      - log: "Testing with missing required payload"
      - post:
          url: "/credentials?size=10&offset=0"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
          expect:
            - statusCode: 400
      - think: 2

      - log: "Testing with invalid payload"
      - post:
          url: "/credentials?size=10&offset=0"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: 123
          expect:
            - statusCode: 400
      - think: 2

      - log: "Testing with invalid URL"
      - post:
          url: "/invalidURL?size=10&offset=0"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 404
      - think: 2
