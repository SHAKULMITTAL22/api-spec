# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "recipient_id"
      - "credential_url"
    skipHeader: true
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Testing various non-success API requests"
    flow:
      - # Testing invalid payload
        post:
          url: "/registrations"
          json:
            recipient_id: "{{ recipient_id }}"
            credential_url: "{{ credential_url }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # expecting a bad request response
        think: 1

      - # Testing unauthenticated request
        get:
          url: "/registrations"
          expect:
            - statusCode: 401 # expecting an unauthorized response
        think: 1

      - # Testing invalid URL
        get:
          url: "/invalid_url"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # expecting a not found response
        think: 1

      - # Testing request to endpoint requiring authentication without providing auth token
        get:
          url: "/registrations"
          headers:
            Token: "" # not providing auth token
          expect:
            - statusCode: 401 # expecting an unauthorized response
        think: 1

      - # Testing request with invalid auth token
        get:
          url: "/registrations"
          headers:
            Token: "invalid_token" # providing invalid auth token
          expect:
            - statusCode: 403 # expecting a forbidden response
        think: 1
