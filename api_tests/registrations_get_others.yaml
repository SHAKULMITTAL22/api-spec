# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "recipient_id"
      - "credential_url"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Scenario: Non-successful API requests"
    flow:
      # Testing an invalid URL
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 404
          think: 1

      # Testing with invalid authentication token
      - get:
          url: "/registrations?size=20&offset=0&recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
          headers:
            Token: "invalid-token"
          expect:
            - statusCode: 401
          think: 1

      # Testing with missing required query parameter
      - get:
          url: "/registrations?size=20&offset=0&credential-url={{ credential_url }}"
          expect:
            - statusCode: 400
          think: 1

      # Testing with invalid query parameter value
      - get:
          url: "/registrations?size=invalid-size&offset=0&recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
          expect:
            - statusCode: 400
          think: 1

      # Testing with exceeding the maximum limit of a query parameter
      - get:
          url: "/registrations?size=51&offset=0&recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
          expect:
            - statusCode: 400
          think: 1
