# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "recipient_id"
      - "credential_url"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
- name: "Testing non-success API responses"
  flow:
    # Test case: Invalid URL
    - get:
        url: "/invalid_url"
        expect:
          - statusCode: 404
        think: 1

    # Test case: Unauthorized access
    - get:
        url: "/registrations"
        expect:
          - statusCode: 401
        think: 1

    # Test case: Missing required header
    - get:
        url: "/registrations"
        headers:
          Token: ""
        expect:
          - statusCode: 401
        think: 1

    # Test case: Invalid payload
    - post:
        url: "/registrations"
        json:
          recipient_id: "invalid_uuid"
          credential_url: "invalid_url"
        expect:
          - statusCode: 400
        think: 1

    # Test case: Invalid query parameters
    - get:
        url: "/registrations?size=invalid&offset=invalid"
        expect:
          - statusCode: 400
        think: 1

    # Test case: Unprocessable entity
    - patch:
        url: "/registrations/invalid_uuid"
        json:
          recipient_id: "new_invalid_uuid"
          credential_url: "new_invalid_url"
        expect:
          - statusCode: 422
        think: 1

    # Test case: Resource not found
    - delete:
        url: "/registrations/invalid_uuid"
        expect:
          - statusCode: 404
        think: 1
