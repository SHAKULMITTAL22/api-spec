# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "recipient_id"
      - "credential_url"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
- name: "Test case: Sending a GET request with an invalid recipient_id"
  flow:
    - get:
        url: "/registrations?recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
        think: 1

- name: "Test case: Sending a GET request with an invalid credential_url"
  flow:
    - get:
        url: "/registrations?recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
        think: 1

- name: "Test case: Sending a GET request without an authentication token"
  flow:
    - get:
        url: "/registrations?recipient-id={{ recipient_id }}&credential-url={{ credential_url }}"
        headers:
          Token: ""
        expect:
          - statusCode: 401
          - hasProperty: "error"
        think: 1

- name: "Test case: Sending a GET request to an invalid URL"
  flow:
    - get:
        url: "/invalid_url"
        expect:
          - statusCode: 404
          - hasProperty: "error"
        think: 1
