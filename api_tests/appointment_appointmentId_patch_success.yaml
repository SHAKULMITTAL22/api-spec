# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_patch for http method type PATCH in artillery framework
# RoostTestHash=33f5f2e325
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "appointment_appointmentId_patch_success.csv"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Patch Appointment Scenario"
    flow:
      - log: "Starting Patch Appointment test scenario"
      - get:
          url: "/appointment/SA-562403"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 200
            hasHeader: "Content-Type"
            headerEquals:
              Content-Type: "application/json"
            hasProperty:
              - order
              - customer
              - appointment
              - address
              - products
              - control
      - think: 
          seconds: 2

      - log: "Patching an Appointment"
      - patch:
          url: "/appointment/SA-562403"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            appointment:
              slot:
                id: "{{ slot_id }}"
              reason: "{{ reason }}"
          expect:
            statusCode: 200
            hasHeader: "Content-Type"
            headerEquals:
              Content-Type: "application/json"
            hasProperty:
              - order.type
              - order.associatedDocument
              - order.addressChange.flag
              - customer.subscriberId
              - appointment.id
              - appointment.startDate
              - appointment.finishDate
              - appointment.priority.flag
              - appointment.priority.reason
              - address.id
              - products.product
              - control.type
              - control.message
              - control.code
      - think: 
          seconds: 2
