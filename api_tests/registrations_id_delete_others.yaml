# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=2c75b43156


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Invalid URL case"
      - get:
          url: "/invalidURL"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Invalid payload case"
      - post:
          url: "/registrations/{{ id }}"
          json:
            invalidField: "invalidValue"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Unauthorized request case"
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "invalidToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Not found request case"
      - get:
          url: "/registrations/invalidId"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
