# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /registrations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=2c75b43156


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Testing GET with incorrect id
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals:
                error: "Registration not found"
        think: 1

      # Testing GET with incorrect URL
      - get:
          url: "/registration/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Testing GET without authorization
      - get:
          url: "/registrations/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
                error: "Unauthorized access"
        think: 1

      # Testing GET with incorrect authorization
      - get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "incorrect_token"
          expect:
            - statusCode: 403
            - hasProperty: "error"
            - equals:
                error: "Forbidden access"
        think: 1

      # Testing GET with invalid id format
      - get:
          url: "/registrations/invalid_id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                error: "Invalid id format"
        think: 1
