# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "grants_get_success.csv"
    fields: ["id", "token", "createdAt", "updatedAt", "account"]
    skipHeader: true

scenarios:
  - name: "Validate successful grants API requests"
    flow:
      - get:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "grants"
            - hasProperty: "total"
            - hasHeader: "api-version"
            - headerEquals: ["api-version", "1.0.0"]
        think: 1
      - get:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "grants.0.id"
            - hasProperty: "grants.0.token"
            - hasProperty: "grants.0.createdAt"
            - hasProperty: "grants.0.updatedAt"
            - hasProperty: "grants.0.account"
            - hasProperty: "grants.0.account.id"
            - hasProperty: "grants.0.account.name"
            - hasProperty: "grants.0.account.image"
            - hasProperty: "grants.0.account.email"
        think: 1
      - get:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - equals: ["{{ grants.0.id }}", "{{ id }}"]
            - equals: ["{{ grants.0.token }}", "{{ token }}"]
            - equals: ["{{ grants.0.createdAt }}", "{{ createdAt }}"]
            - equals: ["{{ grants.0.updatedAt }}", "{{ updatedAt }}"]
            - equals: ["{{ grants.0.account }}", "{{ account }}"]
        think: 1
