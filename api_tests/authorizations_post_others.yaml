# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test various non-successful API responses"
    flow:
      # Invalid email format in payload
      - post:
          url: "/authorizations"
          json:
            email: "invalid_email_format"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"

      - think: 1

      # Missing email in payload
      - post:
          url: "/authorizations"
          json: {}
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"

      - think: 1

      # Invalid API key in the header
      - post:
          url: "/authorizations"
          json:
            email: "valid@example.com"
          headers:
            Token: "InvalidApiKey"
          expect:
            - statusCode: 401
            - hasProperty: "error"

      - think: 1

      # Valid payload but unauthorized (missing Token)
      - post:
          url: "/authorizations"
          json:
            email: "valid@example.com"
          expect:
            - statusCode: 403
            - hasProperty: "error"

      - think: 1

      # Invalid URL case
      - post:
          url: "/invalid_authorizations_endpoint"
          json:
            email: "valid@example.com"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"

      - think: 1
