# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - "email"
  plugins:
    expect: {}
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-successful API requests"
    flow:
      # Testing POST request with invalid email
      - post:
          url: "/authorizations"
          json:
            email: "invalidEmail"
          expect:
            - statusCode: 400
            - hasProperty: "errors.email"
          think: 1

      # Testing POST request without email
      - post:
          url: "/authorizations"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "errors.email"
          think: 1

      # Testing POST request with unauthorized access
      - post:
          url: "/authorizations"
          json:
            email: "{{ email }}"
          headers:
            Token: "invalidToken"
          expect:
            - statusCode: 401
          think: 1

      # Testing POST request with incorrect URL
      - post:
          url: "/incorrectUrl"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 1
