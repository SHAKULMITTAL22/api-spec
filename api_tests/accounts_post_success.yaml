# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account success scenario"
    flow:

      - log: "TEST: Creating a new account with valid email"
      
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdef1"
          payload:
            email: "test@example.com"
          expect:
            - statusCode: 201
            - contentType: json
            - hasHeader: "api-version"
            - headerEquals:
                api-version: "1.0.0"
            - hasHeader: "commit-hash"
            - headerEquals:
                commit-hash: "928d28d"
                
      - think: 2

      - log: "TEST: Creating a new account with another valid email"
        
      - post:
          url: "/accounts"
          headers:
            commit-hash: "xyz1234"
          payload:
            email: "john@test.com"
          expect:
            - statusCode: 201
            - contentType: json
            - hasHeader: "api-version"
            - headerEquals:
                api-version: "1.0.0"
            - hasHeader: "commit-hash"
            - headerEquals:
                commit-hash: "928d28d"
                
      - think: 2
