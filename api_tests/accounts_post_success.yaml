# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Successful Account Creation"
    flow:
      - log: "Starting account creation test with valid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          payload:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals:
                "api-version": "1.0.0"
            - hasHeader: "commit-hash"
            - headerEquals:
                "commit-hash": "928d28d"
            - hasProperty: "email"
            - equals:
                email: "{{ email }}"
      - think: 2
