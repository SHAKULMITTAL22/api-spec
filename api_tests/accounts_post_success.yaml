# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account"
    flow:
      - think: 2
      - log: "Sending POST request to /accounts endpoint with valid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeaders:
                api-version: "1.0.0"
                commit-hash: "928d28d"
