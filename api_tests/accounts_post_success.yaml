# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 5
      name: Warm up
    - duration: 30 
      arrivalRate: 5
      rampTo: 50
      name: Ramp up load
    - duration: 60
      arrivalRate: 50 
      name: Sustained load

scenarios:
  - name: "Account Creation"
    flow:
      - log: "Sending POST request to /accounts endpoint to create a new account with valid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d"
            Authorization: "Bearer {{ AUTH_TOKEN }}"
          payload:
            $ref: "./accounts_post_success.csv"
            skipHeader: true
            delimiter: "^|^"
      - afterResponse: "logResponse"
      - think: 2
      - expect:
          - statusCode: 201
          - hasHeaders:
              - "api-version"
              - "commit-hash"

payloads:
  path: accounts_post_success.csv
  fields:
    - "email"
  delimiter: "^|^"

plugins:
  expect: {}

variables:
  AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

functions:
  logResponse:
    - log: "Received response with status code {{ response.statusCode }}"
