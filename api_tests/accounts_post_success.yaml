# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "accounts_post_success.csv"
    fields: ["email"]
    skipHeader: true
  processor: "./my-functions.js"

scenarios:
  - name: "Create new Account"
    flow:
      - post:
          url: "/accounts"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            commit-hash: "{{$randomString(7, 'abcdefghijklmnopqrstuvwxyz0123456789')}}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasHeader: "commit-hash"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
