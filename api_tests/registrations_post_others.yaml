# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/persons/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=0973c499e9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test non-successful API responses"
    flow:
      # Testing with invalid id
      - post:
          url: "/recipients/persons/invalid-id/registrations"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 404
          think: 2
      # Testing with missing required field
      - post:
          url: "/recipients/persons/{{ id }}/registrations"
          json:
            reference: "{{ reference }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 400
          think: 2
      # Testing with invalid URL
      - post:
          url: "/invalid-url/{{ id }}/registrations"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 404
          think: 2
      # Testing with unauthorized access
      - post:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "invalid-token"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 403
          think: 2
