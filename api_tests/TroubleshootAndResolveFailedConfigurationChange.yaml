# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4-turbo
# 
# ROOST_METHOD_HASH=6105ffde10
# ROOST_METHOD_SIG_HASH=e3af0b476f
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: WiFi Troubleshooting for Fiber Internet Customers',
#   background: 'Background:\n' +
#     '    Given the customer support representative has access to the service activation and configuration API\n' +
#     '    And the customer support representative has access to the service test management API',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Troubleshoot and resolve a failed configuration change',
#     steps: 'Given a customer with subscriber ID "SVTAL006" requests a WiFi configuration change\n' +
#       'When the support representative uses the configuration API to make the requested change\n' +
#       'And the configuration API returns an error response\n' +
#       'Then the support representative should initiate a diagnostic test\n' +
#       'And the service test API should return a test ID\n' +
#       'And the diagnostic result should provide details about the configuration failure\n' +
#       'When the support representative resolves the identified issue and retries the configuration change\n' +
#       'Then the configuration API should return a success response\n' +
#       "And the customer's WiFi should reflect the requested changes",
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    development:
      API_HOST: "http://localhost:3000"
      AUTH_TOKEN: "Bearer your_development_auth_token"
    production:
      API_HOST: "https://api.production.example.com"
      AUTH_TOKEN: "Bearer your_production_auth_token"
  payload:
    path: "TroubleshootAndResolveFailedConfigurationChange.csv"
    fields:
      - subscriberID
      - configurationPayload
      - testPayload
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Troubleshoot and resolve a failed configuration change"
    flow:
      - post:
          url: "/configuration"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            subscriberID: "{{ subscriberID }}"
            configuration:
              action:
                type: "{{ configurationPayload.action.type }}"
              id: "{{ configurationPayload.id }}"
              adminStatus: "{{ configurationPayload.adminStatus }}"
              startDate: "{{ configurationPayload.startDate }}"
              endDate: "{{ configurationPayload.endDate }}"
              state: "{{ configurationPayload.state }}"
          log: "Attempting to apply configuration change for subscriber SVTAL006"
          expect:
            - statusCode: 400
            - hasProperty: "error"
          think: 1

      - log: "Configuration API returned an error response as expected, initiating diagnostic test"

      - post:
          url: "/serviceTest"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            serviceTest:
              type: "{{ testPayload.type }}"
              customer:
                subscriberId: "{{ subscriberID }}"
          log: "Sending service test request for diagnostics"
          expect:
            - statusCode: 200
            - hasProperty: "testID"
          think: 1

      - log: "Service test API returned a test ID, checking for diagnostic details"

      - post:
          url: "/configuration"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            subscriberID: "{{ subscriberID }}"
            configuration:
              action:
                type: "{{ configurationPayload.action.type }}"
              id: "{{ configurationPayload.id }}"
              adminStatus: "{{ configurationPayload.adminStatus }}"
              startDate: "{{ configurationPayload.startDate }}"
              endDate: "{{ configurationPayload.endDate }}"
              state: "{{ configurationPayload.state }}"
          log: "Retrying configuration change after resolving issues"
          expect:
            - statusCode: 200
          think: 1

      - log: "Configuration change applied successfully, WiFi should now reflect the requested changes"
