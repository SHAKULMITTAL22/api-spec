# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type DBRX and AI Model dbrx-instruct
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "./login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Login using OTP
    flow:
      - log:
          message: "Login using OTP: creates a session. Email: {{ email }}, OTP: {{ otp }}, Client: {{ client }}"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          body: '{ "email": "{{ email }}", "otp": "{{ otp }}", "client": "{{ client }}" }'
      - think: 1
