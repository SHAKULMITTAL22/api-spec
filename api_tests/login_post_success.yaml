# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type DBRX and AI Model dbrx-instruct
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Login using OTP: creates a session"
    flow:
      - log:
          text: "Login Request: {{ $processEnvironment.EMAIL }} | {{ email }} | {{ otp }} | {{ client }}"
      - post:
          url: "/login"
          headers:
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          payload:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
      - think: 5
      - expect:
          statusCode: 201
          headers:
            - "api-version"
          body:
            required:
              - "token"
          hasProperty:
            - "token"
          headerEquals:
            api-version: "1.0.0"
          jmespath:
            token: "[?contains(@, ':') == `true`]"
      - capture:
          - json: "$.token"
            as: "token"
      - log:
          text: "Login Response: {{ token }}"
