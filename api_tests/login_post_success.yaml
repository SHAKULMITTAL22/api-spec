# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 1
      name: Warm up
    - duration: 30
      arrivalRate: 2
      rampTo: 5
      name: Ramp up load
    - duration: 60
      arrivalRate: 5
      name: Sustained load

scenarios:
  - name: Login with OTP
    flow:
      - post:
          url: "/login"
          beforeRequest: |
            console.log("Sending a POST request to /login endpoint to create a session using OTP");
          afterResponse: |
            console.log("Received response from /login endpoint");
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - contentType: json
            - hasProperty: "token"
            - equals:
                - "token.length"
                - 73
          payload:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.token"
            as: "token"
      - log: "Logged in successfully. Token: {{ token }}"
      - think: 2

payload:
  path: login_post_success.csv
  fields:
    - "email"
    - "otp"
    - "client"
  skipHeader: true
  delimiter: "^|^"
