# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 1
      name: Warm up

scenarios:
  - name: "Login API Failure Tests"
    flow:
      - log: "Sending login request with invalid email format"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalidemail"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "INVALID_EMAIL_FORMAT"
        think: 2

      - log: "Sending login request with missing required field"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json  
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "MISSING_REQUIRED_FIELD"
        think: 2
        
      - log: "Sending login request with invalid client type"
        post:
          url: "/login" 
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "invalidclient"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "error"
            - equals:
                - "error.code"
                - "INVALID_CLIENT_TYPE"
        think: 2

      - log: "Sending login request with incorrect URL"
        post:
          url: "/incorrect-login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 404
        think: 2
