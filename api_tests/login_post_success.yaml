# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 5
  plugins:
    expect: {}

scenarios:
  - name: "Login using OTP: creates a session"
    flow:
      - log:
          message: "Logging in with OTP"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          payload:
            delimiter: "^|^"
            skipHeader: true
            dataFromFile: "login_post_success.csv"
      - think:
          duration: 5
      - expect:
          - statusCode: 201
          - hasHeader: "api-version"
          - headerEquals:
              - "api-version"
              - "1.0.0"
