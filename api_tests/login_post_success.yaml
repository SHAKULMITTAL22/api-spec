# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 1
      name: Warm up
    - duration: 30
      arrivalRate: 2
      rampTo: 5
      name: Ramp up load
    - duration: 60
      arrivalRate: 5
      name: Sustained load

scenarios:
  - name: "Login API Failure Tests"
    flow:
      - log: "Test case: Missing required fields in payload"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            client: "web"
        expect:
          - statusCode: 400
          - contentType: json
          - hasProperty: "error"
          - equals:
              - "error.message"
              - "Required fields missing in payload"
        think: 2

      - log: "Test case: Invalid email format"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            client: "web"
            email: "invalidemail"
            otp: "123456"  
        expect:
          - statusCode: 400
          - contentType: json
          - hasProperty: "error"
          - equals:
              - "error.message"
              - "Invalid email format"
        think: 2
        
      - log: "Test case: Invalid client type"
        post:
          url: "/login" 
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            client: "invalidclient"
            email: "test@example.com"
            otp: "123456"
        expect:
          - statusCode: 400
          - contentType: json
          - hasProperty: "error"
          - equals:
              - "error.message"
              - "Invalid client type"
        think: 2
        
      - log: "Test case: Incorrect OTP"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            client: "web"
            email: "test@example.com"
            otp: "111111"
        expect:
          - statusCode: 401
          - contentType: json
          - hasProperty: "error"
          - equals:
              - "error.message"
              - "Incorrect OTP"
        think: 2

      - log: "Test case: Expired OTP"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            client: "web" 
            email: "test@example.com"
            otp: "123456"
        expect:
          - statusCode: 401  
          - contentType: json
          - hasProperty: "error"
          - equals:
              - "error.message"
              - "OTP expired"
        think: 2
