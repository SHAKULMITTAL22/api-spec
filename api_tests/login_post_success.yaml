# ********RoostGPT********

# Test generated by RoostGPT for test api-test using AI Type Claude AI and AI Model claude-3-opus-20240229
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 10
      arrivalRate: 1
      name: Warm up
    - duration: 30
      arrivalRate: 2
      rampTo: 5
      name: Ramp up load
    - duration: 60
      arrivalRate: 5
      name: Sustained load

scenarios:
  - name: "Login API Failure Tests"
    flow:
      - log: "Attempting login with invalid email format"
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "invalidemail"
            otp: "123456"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "message"
            - equals:
                - "message": "Invalid email format"

      - think: 2

      - log: "Attempting login with missing required field"  
        post:
          url: "/login"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            client: "web"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "message"
            - equals:
                - "message": "Missing required field: otp"

      - think: 2
        
      - log: "Attempting login with invalid client type"
        post:
          url: "/login" 
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"  
            client: "invalid"
          expect:
            - statusCode: 400
            - contentType: json
            - hasProperty: "message"
            - equals:
                - "message": "Invalid client type"
                
      - think: 2
        
      - log: "Attempting login with incorrect URL"
        post:
          url: "/wrong-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "test@example.com"
            otp: "123456"
            client: "web"  
          expect:
            - statusCode: 404
