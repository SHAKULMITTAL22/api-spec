# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4
# 
# ================================VULNERABILITIES================================
# Vulnerability: Exposure of Sensitive Information in URL (CWE-598)
# Issue: Potential sensitive information (ID or token) revealed in the URL, as seen in the log entry '[HTTP SERVER] get /credentials/497f6eca-6276-4993-bfeb-53cbbbba6f08'
# Solution: Avoid revealing sensitive data in URLs. If necessary, ensure that the data is encrypted or use POST method instead of GET to avoid exposure in logs and browser history.
# 
# ================================================================================
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    order: sequence

scenarios:
  - name: "Validate successful API responses"
    flow:
      - log: "Testing GET /credentials/{id} endpoint"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
      - think: 1
