# ********RoostGPT********
# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4

# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./authorizations_id_delete_others.csv"
    skipHeader: true
    fields:
      - id
  phases:
    - duration: 60
      arrivalRate: 5
  processor: "./my-functions.js"

scenarios:
  - name: Non-successful API requests
    flow:
      # Test case with invalid token
      - log: "Delete request with invalid token"
        delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "InvalidToken"
        expect:
          - statusCode: 401 # Unauthorized

      - think: 1

      # Test case with invalid id
      - log: "Delete request with invalid id"
        delete:
          url: "/authorizations/InvalidId"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404 # Not Found

      - think: 1

      # Test case with missing id
      - log: "Delete request with missing id"
        delete:
          url: "/authorizations/"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400 # Bad Request

      - think: 1

      # Test case with valid id but not authorized to delete
      - log: "Delete request with valid id but not authorized to delete"
        delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 403 # Forbidden

      - think: 1
