# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      # Test case: DELETE request with incorrect URL
      - think: 1
      - delete: "/incorrect_url/{{ id }}"
        headers:
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 404 # Expecting 404 Not Found status code

      # Test case: DELETE request with invalid id
      - think: 1
      - delete: "/authorizations/invalid_id"
        headers:
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 400 # Expecting 400 Bad Request status code

      # Test case: DELETE request with missing authorization token
      - think: 1
      - delete: "/authorizations/{{ id }}"
        expect:
          - statusCode: 401 # Expecting 401 Unauthorized status code

      # Test case: DELETE request with incorrect authorization token
      - think: 1
      - delete: "/authorizations/{{ id }}"
        headers:
          Token: "incorrect_token"
        expect:
          - statusCode: 403 # Expecting 403 Forbidden status code
