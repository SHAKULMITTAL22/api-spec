# Test generated by RoostGPT for test api-test using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - delete: # Test case: DELETE request without authorization header
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401
          think: 1

      - delete: # Test case: DELETE request with invalid authorization header
          url: "/authorizations/{{ id }}"
          headers:
            Token: "InvalidToken"
          expect:
            - statusCode: 403
          think: 1

      - delete: # Test case: DELETE request with valid authorization header but invalid id
          url: "/authorizations/InvalidId"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1

      - delete: # Test case: DELETE request with valid authorization header and id
          url: "/authorizations/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
          think: 1
