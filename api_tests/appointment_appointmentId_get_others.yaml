# ********RoostGPT********

# Test generated by RoostGPT for test API-test-Vtal using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment/{appointmentId}_get for http method type GET in artillery framework
# RoostTestHash=b69bb60b36
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_appointmentId_get_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Responses for Retrieve Appointment"
    flow:
      - log: "Testing invalid appointment ID format"
      - get:
          url: "/appointment/invalid-id"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 
          seconds: 2

      - log: "Testing missing authorization header"
      - get:
          url: "/appointment/SA-562403"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 
          seconds: 2

      - log: "Testing non-existing appointment ID"
      - get:
          url: "/appointment/999999999999999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 
          seconds: 2

      - log: "Testing invalid URL"
      - get:
          url: "/invalid/endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 
          seconds: 2
